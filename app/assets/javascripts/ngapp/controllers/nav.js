'use strict';

hnApp.controller('NavCtrl', function($scope, $modal, $log) {

  /*$scope.open = function() {
    var modalInstance = $modal.open({
      backdrop: 'static',
      templateUrl: '/templates/submit_posts.html',
      controller: 'PostsCtrl'
    });
  };*/

});
